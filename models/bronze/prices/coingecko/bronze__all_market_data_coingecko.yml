version: 2

models:
  - name: bronze__all_market_data_coingecko
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - ID
            - _INSERTED_DATE 
    columns:
      - name: ID
        tests:
          - not_null
      - name: _INSERTED_DATE
        tests:
          - not_null
      - name: DATA
        tests:
          - not_null