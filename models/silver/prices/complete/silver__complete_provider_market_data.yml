version: 2

models:
  - name: silver__complete_provider_market_data
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - ASSET_ID
            - RECORDED_DATE
            - PROVIDER
    columns:
      - name: ASSET_ID
        tests:
          - not_null
      - name: RECORDED_DATE
        tests:
          - not_null
      - name: PROVIDER
        tests:
          - not_null